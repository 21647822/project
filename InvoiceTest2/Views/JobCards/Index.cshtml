@model IEnumerable<InvoiceTest2.Models.JobCard>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PartsUsed)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FeesIncluded)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TotalAmount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PhoneNo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PartsUsed)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FeesIncluded)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TotalAmount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PhoneNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.JobID }) |
            @*@Html.ActionLink("Details", "Details", new { id = item.JobID }) |*@
            @Html.ActionLink("Delete", "Delete", new { id = item.JobID }) |
            @Html.ActionLink("View Invoice", "Invoice", new { id = item.JobID })
        </td>
    </tr>
}

</table>
